---
version: 1
name: Zhizhang-design-system
description: 织章是一款面向长篇网文创作的本地优先 AI 写作工作台。设计取雾、书页与梅花的意象——雾灰绿承载工具层，暖白留给稿纸与高层卡片，墨色文字，梅红作主强调，配纸纹与梅枝的克制装饰。界面是长时间停留的写作工具，不是营销页面：信息密度高、对比克制、装饰只在空白处出现，绝不与正文争注意力。全部取值以 CSS 自定义属性落在 desktop-app/src/App.css 的 :root，浅深两套主题只换取值不换语义。
---

# 织章设计系统

> 权威取值在 `desktop-app/src/App.css` 的 `:root` 与 `:root[data-theme="dark"]`。
> 本文件描述取值背后的意图与用法；改设计先改这里，再改 CSS。
> `docs/references/notion-design-reference.md` 是外部格式参考，不是本系统的依据。

## 总则

1. **写作工具，不是落地页。** 信息密度优先于留白排场。同一屏要能放下目录、正文和 Agent 对话。
2. **装饰退到背后。** 纸纹、梅枝、分隔线的浓度都由主题变量统一控制，任何装饰都不接收指针事件。
3. **语义不随主题变。** 一个色的角色（前景 / 描边 / 底纹）在浅深两套主题里是同一个，只有取值不同。
4. **不联网取资源。** 应用离线优先：字体只用系统已装的，装饰一律 SVG 或 CSS 生成，
   操作图标是内联 SVG。唯一的位图是品牌标识，它随包发布、由脚本从母图生成，不走网络。

## 配色

### 结构色

底色分四层，从外到内逐层变亮（墨夜主题下逐层变暗）：

| 变量 | 书页 | 墨夜 | 用途 |
|---|---|---|---|
| `--bg` | `#E7ECE9` | `#15120D` | 应用最外层底 |
| `--surface` | `#F4F5F2` | `#1D1913` | 侧栏、面板 |
| `--surface-raised` | `#FFFFFF` | `#252019` | 卡片、弹窗 |
| `--surface-sunken` | `#DFE5E1` | `#110E0A` | 输入框、嵌套区、分栏手柄 |

描边两级：`--line` 常规分隔，`--line-strong` 需要看清边界时（输入框、次按钮）。

文字六级，从 `--text` 到 `--text-6` 逐级变弱。标题用 `--text`，正文 `--text-2`/`--text-3`，
辅助说明 `--text-4`/`--text-5`，禁用与占位 `--text-6`。

### 语义色

每个语义族固定四个角色，写新组件时按角色选，不要按明暗猜：

| 角色 | 含义 |
|---|---|
| `--x` | 主色，图标、实心块、进度条 |
| `--x-fg` | 前景，写在普通底色上的文字 |
| `--x-line` | 描边 |
| `--x-bg` | 底纹，衬在文字后面的浅块 |

| 族 | 书页主色 | 墨夜主色 | 语义 |
|---|---|---|---|
| `--accent` | `#A5424E` 梅红 | `#DD7057` | 主操作、当前选中、品牌标识 |
| `--accent-2` | `#346676` 靛青 | `#6FA8C8` | 信息、链接、次级强调 |
| `--accent-3` | `#6B5B95` 紫毫 | `#A392CE` | 少量特性标记，用量克制 |
| `--ok` | `#4A7C59` 竹绿 | `#79A886` | 通过、成功、已完成 |
| `--warn` | `#9A6D14` 藤黄 | `#D2A64E` | 提醒、需要注意 |
| `--danger` | `#A3342E` 赭赤 | `#D9695F` | 危险、失败、删除 |

`--accent-strong` 是主色的按下态；`--accent-tint` 与 `--accent-ring` 是半透明版，
分别用于选中底纹和焦点环。

### 正文纸

`--paper-bg` / `--paper-ink` / `--paper-caret` 单独一组，只服务章节编辑器、大纲卡片和阅读浮层。
设置里的「纸张模式」在任何主题下都强制给一块暖白稿纸。

### 禁忌

- 不要用 `--accent` 作大面积背景，梅红只做点。
- 不要混用 `--accent`（操作）与 `--accent-2`（信息），角色不同。
- 不要在组件里写死十六进制色。唯一例外是 `.tag-option-icon.*` 的 13 种标签分类色——
  那是用户自己挑的分类色，类名即颜色名，不属于语义体系。
- 不要给平铺的列表项加阴影，用 `--line` 描边。

## 字体与字阶

界面用系统无衬线，正文默认书籍宋体（可在设置改），都不引 Web Font。

| 变量 | 字号 | 行高 | 字距 | 用途 |
|---|---|---|---|---|
| `--fs-xl` | 21px | `--lh-xl` 1.25 | 0 | 页面标题 |
| `--fs-lg` | 17px | `--lh-lg` 1.35 | 0 | 区块标题、弹窗标题 |
| `--fs-md` | 14px | `--lh-md` 1.55 | 0 | 导航、正文控件 |
| `--fs-sm` | 12px | `--lh-sm` 1.5 | 0 | 按钮、列表项、大部分界面文字 |
| `--fs-xs` | 11px | `--lh-xs` 1.45 | 0 | 辅助说明、元信息 |

界面字距统一为 0。9px 和 10px 只在角标、图例这类极小场合出现，不进字阶。

## 间距

4px 基数，阶梯为 **4 / 6 / 8 / 10 / 12 / 16 / 20 / 24 / 28 / 32 / 40 / 48**。

选值不用变量，直接写 px——间距的价值在于取值一致，加一层变量只增加间接层不改变渲染。
1–3px 是发丝级间隙（图标与文字之间、徽章内缩），48px 以上是页面级留白，都不受阶梯约束。

常用档位：列表项内边距 6–8，卡片 12–16，面板 16–20，页面 24–32。

## 圆角

| 变量 | 值 | 用途 |
|---|---|---|
| `--r-sm` | 6px | 按钮、输入框、列表项、小面板 |
| `--r-md` | 10px | 卡片、工作区容器 |
| `--r-lg` | 16px | 弹窗、浮层 |
| `--r-full` | 999px | 状态标签、计数徽章、分段控件、模式切换 |

`--r-full` 只给真正的胶囊形，普通按钮一律 `--r-sm`。

## 层次

| 变量 | 用途 |
|---|---|
| 不写阴影 | 平铺的列表项、表格行，只用 `--line` 描边 |
| `--shadow-1` | 卡片常态 |
| `--shadow-2` | 卡片悬停、抬起态 |
| `--shadow-3` | 抽屉、侧边浮层 |
| `--shadow-4` | 弹窗、模态 |

书页主题的投影用低饱和墨绿灰（`rgba(43, 57, 49, …)`），纯黑压在浅色界面上会显脏。

## 装饰

### 纸纹

`feTurbulence` 生成的纤维噪点铺在 `.app::before`，浓度由 `--grain-opacity` 控制。
书页主题下 `mix-blend-mode: multiply` 压出颗粒，墨夜主题下换 `screen` 自动变淡。
正文区另叠一层 27px 周期的横向抄网纹。

### 梅枝

`PlumBranch` 组件（`features/editor/plum-branch.tsx`）。枝条取 `--branch-ink`，
花瓣取 `--plum-ink`，浓度分别由 `--branch-opacity` 与 `--plum-opacity` 控制。

画法要点，改几何时别丢：

- 枝条是**三段硬折**，不是圆弧。圆弧会像藤蔓，梅枝的性格在于折角。
- 花**小而疏**，花瓣半径不超过 `r * 0.5`，中心偏移 `r * 0.7` 保证瓣不重叠。花瓣一大就糊成红团。
- 花蕊短，`r * 0.66` 以内，再长就像触角。

落位：启动页、空项目首页、剧情搜索空状态各一枝 `lg`；主侧栏页脚一枝 `sm` 并水平翻转。
全部 `pointer-events: none`。

### 页眉分隔

`.page-header::after` 是一条由粗到细、右端收笔的墨线，模拟毛笔提锋，不用等宽实线。

## 组件

### 图标

图标分两类，不要混用：

| 类型 | 载体 | 用在哪 |
|---|---|---|
| 品牌标识 | 位图 `public/zhizhang-brand.png` | 启动页 64px、侧栏印记 38px、应用图标与 favicon |
| 操作图标 | `components/icon.tsx` 的内联 SVG | 导航、按钮、页签、空状态，一切可点的地方 |

品牌图要的是质感（纸纤维、编织、梅花），位图才画得出来；操作图标要的是小尺寸下的
清晰度和跟随主题变色，只有矢量做得到。反过来都不成立——位图缩到 14px 会糊，
矢量画不出纸的肌理。

品牌图的母图在 `branding/zhizhang-brand.png`（1024×1024），全套派生资源由
`src-tauri/icons/create_icons.sh` 一次生成。界面里那张只到 256px，够 4 倍屏用；
搬 1024 的母图进包只是白白多 1.7MB。品牌图自带暖纸底和圆角，
容器只负责按 22% 裁切，不要再叠一层描边或底色。

操作图标统一 24 的 viewBox、1.8 描边、只描边不填充、颜色取 `currentColor`——
所以它放进任何按钮都自动跟随该按钮的文字色，CSS 里不要单独给图标指定颜色。

尺寸按层级选，同一层级必须一致：

| 档位 | 用途 |
|---|---|
| 17 | 主导航 |
| 15–16 | 次级入口、弹窗关闭、返回 |
| 13–14 | 工具条、编辑器页签、列表项操作 |
| 11–12 | 标签角标、内联状态 |
| 26–42 | 空状态与检索页的大图标 |

图标与文字之间留 4–8px（发丝级到一档），由所在按钮的 `gap` 控制，不给图标加 margin。
纯图标按钮没有文字兜底，必须自带 `title` 与 `aria-label`。

新增图标前先翻一遍 `icon.tsx` 里已有的，能复用就复用。**不要出现两个语义不同但形状雷同的图标**：
「扫榜」和「统计」一度都是三根柱子，只能靠位置区分，等于没有图标。

分类标签的彩色字符（`.tag-option-icon.*`）是用户自选的分类标记，不属于本体系，别去统一。

### 按钮

只有三种。同一屏不应出现两个填充按钮。

| 类名 | 形态 | 用途 |
|---|---|---|
| `.btn-primary` | 梅红填充，文字 `#FFF8EE` | 当前屏唯一的主操作 |
| `.btn-secondary` | `--surface-raised` 底 + `--line-strong` 描边 | 并列的次要操作 |
| `.btn-ghost` | 透明，悬停出 `--line` 底 | 取消、关闭、第三级操作 |

统一 34px 高、`--r-sm` 圆角、`--fs-sm` 字号、`focus-visible` 出 2px 焦点环。
粗指针设备（`pointer: coarse`）下提到 44px。

### 输入

`--surface-sunken` 底、`--line-strong` 描边、`--r-sm` 圆角、34px 高。
聚焦时描边转 `--accent`，外加 3px `--accent-ring` 焦点环。

### 选中态

不要用饱和实心块。统一为 `--accent-tint` 半透明底纹 + `--accent-fg` 文字；
主导航另加 3px 宽的左侧指示条。

### 分栏手柄

`PaneResizer`（`features/editor/pane-resizer.tsx`）。视觉宽 5px，命中区外扩 3px；
悬停或聚焦时转 `--accent`；双击复位到默认档；方向键可微调。
取值区间集中登记在 `features/editor/panes.ts`。

## 响应式

| 断点 | 关键变化 |
|---|---|
| ≥ 1100px | 完整三栏：侧栏 + 正文 + Agent 面板 |
| 820–1100px | Agent 面板转浮层 |
| 640–820px | 编辑器侧栏转顶部横向标签条 |
| < 640px | 单列堆叠；主导航移到底部；全部分栏手柄隐藏 |

移动端不维护第二套色板，`--m-*` 全部引用主题变量。

## 迭代方式

1. 一次只改一个组件。
2. 改配色改 `:root` 的取值，不要在组件里写死。新增语义族要凑齐四个角色。
3. 间距从阶梯里选，圆角与层次从表里选。
4. 改完跑 `npm --prefix desktop-app run build`，并在浅深两套主题下各看一遍。
5. 本文件与 CSS 不一致时以 CSS 为准，然后回来更正本文件。

## 已知缺口

- 动效时长没有统一，现有过渡在 .16s–.2s 之间，建议后续统一到 .16s ease。
- 表单校验的成功态没有专门样式，目前只有错误态。
- 图表配色（用量面板）还没纳入语义体系，仍是页面自有取值。
- 键盘焦点环只在按钮和输入上实现，列表项与卡片尚未覆盖。
- 图标集没有做视觉重量校准，闭合形状（禁词、AI 检测）比开放线条（搜索、导出）显重。
